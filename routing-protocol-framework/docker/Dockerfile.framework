FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src
RUN pip install -U pip && pip install -e .

CMD ["python", "-m", "rpf.cli.main", "run", "--config", "configs/experiments/failure_recovery.yaml"]
