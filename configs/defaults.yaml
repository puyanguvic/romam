seed: 42
tick: 1
logging:
  level: INFO
  jsonl: true
output_dir: results/runs
engine: tick
engine_config: configs/engines/tick.yaml
protocol: ospf
protocol_config: configs/protocols/ospf.yaml
topology:
  type: ring
  n_nodes: 8
  default_metric: 1.0
experiment:
  max_ticks: 80
  failures: []
